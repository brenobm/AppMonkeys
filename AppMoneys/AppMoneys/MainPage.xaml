<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AppMoneys"
             x:Class="AppMoneys.MainPage">

    <StackLayout>
        <Button Text="Navegação Não Modal" Clicked="ButtonNaoModal_Clicked" />
        <Button Text="Navegação Modal" Clicked="ButtonModal_Clicked" />
        <Button Text="Voltar Modal" Clicked="ButtonVoltar_Clicked" />
        <Entry Text="{Binding SearchTerm}" />
        <Button Text="Pesquisar" Command="{Binding SearchCommand}" />
        <ListView ItemsSource="{Binding Results}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding Description}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>